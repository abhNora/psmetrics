% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SPECKS.R
\name{SPECKS}
\alias{SPECKS}
\title{Compute the SPECKS utility metric}
\usage{
SPECKS(OD, SD, split = 0.5, alpha = 0.05, seed = NULL)
}
\arguments{
\item{OD}{A data frame containing the original data.}

\item{SD}{A data frame containing the synthetic data.}

\item{split}{Proportion of the combined sample used for training.}

\item{alpha}{Significance level used to compute the critical threshold.}

\item{seed}{Optional integer seed for reproducible sample splitting.}
}
\value{
A named list with:
\describe{
\item{metric}{Character string with the metric name.}
\item{value}{Observed SPECKS value.}
\item{threshold}{Critical value for the Kolmogorov-Smirnov comparison.}
\item{H0_accepted}{Logical value indicating whether the null hypothesis is accepted.}
\item{p_value}{Associated p-value from the KS test.}
}
}
\description{
Computes the SPECKS metric comparing an original dataset (\code{OD}) and a
synthetic dataset (\code{SD}). To reduce overfitting, the propensity model is fit
on a training subset and evaluated on a test subset.
}
\details{
SPECKS is computed as the maximum absolute difference between the empirical
cumulative distribution functions of propensity scores for original and
synthetic records in the test split.
}
\examples{
set.seed(123)
OD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))
SD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))

SPECKS(OD, SD, seed = 123)

}
