% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pMSE.R
\name{pMSE}
\alias{pMSE}
\title{Compute the pMSE utility metric}
\usage{
pMSE(OD, SD, alpha = 0.05)
}
\arguments{
\item{OD}{A data frame containing the original data.}

\item{SD}{A data frame containing the synthetic data.}

\item{alpha}{Significance level used to compute the critical threshold.}
}
\value{
A named list with:
\describe{
\item{metric}{Character string with the metric name.}
\item{value}{Observed pMSE value.}
\item{threshold}{Critical value for the test under the null hypothesis.}
\item{H0_accepted}{Logical value indicating whether the null hypothesis is accepted.}
\item{p_value}{Associated p-value.}
}
}
\description{
Computes the propensity-score mean squared error (pMSE) comparing an
original dataset (\code{OD}) and a synthetic dataset (\code{SD}). This implementation
uses the full combined sample to fit the propensity model, as in the source
code provided for the paper.
}
\details{
The function stacks \code{OD} and \code{SD}, creates a binary label (\code{0} for \code{OD},
\code{1} for \code{SD}), fits a logistic regression, and computes pMSE as:

\code{pMSE = (1 / N) * sum((propensity - c)^2)}

where \code{c} is the proportion of synthetic records in the combined sample.
}
\examples{
set.seed(123)
OD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))
SD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))

pMSE(OD, SD)

}
