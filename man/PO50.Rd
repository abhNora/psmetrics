% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PO50.R
\name{PO50}
\alias{PO50}
\title{Compute the PO50 utility metric}
\usage{
PO50(OD, SD, split = 0.5, alpha = 0.05, seed = NULL)
}
\arguments{
\item{OD}{A data frame containing the original data.}

\item{SD}{A data frame containing the synthetic data.}

\item{split}{Proportion of the combined sample used for training.}

\item{alpha}{Significance level used to compute the critical thresholds.}

\item{seed}{Optional integer seed for reproducible sample splitting.}
}
\value{
A named list with:
\describe{
\item{metric}{Character string with the metric name.}
\item{value}{Observed PO50 value.}
\item{threshold}{Named numeric vector with lower and upper critical bounds.}
\item{H0_accepted}{Logical value indicating whether the null hypothesis is accepted.}
\item{p_value}{Associated two-sided p-value.}
}
}
\description{
Computes the PO50 metric comparing an original dataset (\code{OD}) and a
synthetic dataset (\code{SD}). To reduce overfitting, the propensity model is fit
on a training subset and evaluated on a test subset.
}
\details{
After training the propensity model on the training split, records in the
test split are classified using the estimated propensity scores. The PO50
value is then computed from the percentage of correctly classified records,
rescaled by subtracting 50.
}
\examples{
set.seed(123)
OD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))
SD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))

PO50(OD, SD, seed = 123)

}
