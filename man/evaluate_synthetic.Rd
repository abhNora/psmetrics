% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_synthetic.R
\name{evaluate_synthetic}
\alias{evaluate_synthetic}
\title{Compute pMSE, SPECKS and PO50 in one call}
\usage{
evaluate_synthetic(
  OD,
  SD,
  alpha = 0.05,
  split = 0.5,
  seed = NULL,
  include_threshold = FALSE,
  include_decision = FALSE
)
}
\arguments{
\item{OD}{A data frame containing the original data.}

\item{SD}{A data frame containing the synthetic data.}

\item{alpha}{Significance level used in all three metrics.}

\item{split}{Proportion of the combined sample used for training in \code{SPECKS}
and \code{PO50}.}

\item{seed}{Optional integer seed for reproducible sample splitting in
\code{SPECKS} and \code{PO50}.}

\item{include_threshold}{Logical; if \code{TRUE}, includes the threshold(s) for
each metric in the output.}

\item{include_decision}{Logical; if \code{TRUE}, includes \code{H0_accepted} for each
metric in the output.}
}
\value{
A named list with one element per metric: \code{pMSE}, \code{SPECKS}, and
\code{PO50}.
}
\description{
Convenience wrapper that computes the three propensity score-based utility
metrics comparing original data (\code{OD}) and synthetic data (\code{SD}).
}
\details{
By default, the function returns only the metric value and p-value for each
measure. Analysts can optionally request the critical threshold and/or the
null-hypothesis decision.
}
\examples{
set.seed(123)
OD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))
SD <- data.frame(x = rnorm(100), y = rbinom(100, 1, 0.5))

evaluate_synthetic(OD, SD)
evaluate_synthetic(OD, SD, include_threshold = TRUE, include_decision = TRUE)

}
